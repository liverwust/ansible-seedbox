---
- name: Directories permissions
  file:
    path: "{{ item }}"
    state: directory
    owner: p2p
    group: p2p
  with_items:
    - "{{ radarr_downloads }}"

- name: Configure optional network links (radarr)
  set_fact:
    radarr_network_links: "{{ radarr_network_links | default([]) }} + [item]"
  with_items:
    - rtorrent:rtorrent
  when: use_containers.rtorrent

- name: Configure optional network links (radarr)
  set_fact:
    radarr_network_links: "{{ radarr_network_links | default([]) }} + [item]"
  with_items:
    - jackett:jackett
  when: use_containers.jackett

- name: Radarr
  docker_container:
    name: radarr
    image: linuxserver/radarr:latest
    state: started
    hostname: radarr
    pull: yes
    restart_policy: on-failure
    restart_retries: 2
    volumes:
      - "/etc/radarr:/config"
      - "{{ medias_root }}:/p2p"
      - "/etc/localtime:/etc/localtime:ro"
    env:
      PUID: "{{ p2p_uid }}"
      PGID: "{{ group_gid }}"
    purge_networks: yes
    networks:
      - name: p2p
        links: "{{ radarr_network_links }}"
    labels:
      traefik.docker.network: "p2p"
      traefik.enable: "true"
      traefik.basic.frontend.rule: "Host:radarr.{{ domain }}"
      traefik.basic.port: "7878"
